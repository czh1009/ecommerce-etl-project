# 房地产市场数据分析项目 (Real Estate Market Analysis)

本项目是一个基于 Python 的数据分析课题，旨在通过对房产销售数据进行清洗、处理和可视化，探索房地产市场的核心指标（如价格、面积、城市分布等）及其内在联系。

## 1. 项目简介

本项目使用 Jupyter Notebook 环境，对抓取的房产销售数据进行深度挖掘。主要分析内容包括各城市房价差异、房屋属性（面积、朝向等）与价格的相关性，以及市场供应情况的整体画像 。  



## 2. 数据集说明

项目核心数据存储在 `data/house_sales.csv` 中。

### 字段定义：

- **city**: 房产所在城市   

- **province**: 房产所在省份   

- **address**: 详细地址   

- **name**: 小区或项目名称   

- **area**: 房屋面积（㎡）   

- **price**: 房屋总价（如“128万”）   

- **unit**: 房屋单价（元/㎡）   

- **rooms**: 户型（如“3室2厅”）   

- **floor**: 楼层信息（含总楼层）   

- **toward**: 房屋朝向（如“南北向”）   

- **year**: 建筑年代   

- **origin_url**: 数据来源链接   

  

  

## 3. 环境配置

运行本项目需要安装以下 Python 环境及第三方库：

### 必备环境：

- Python 3.x
- Jupyter Notebook 或 JupyterLab

### 主要依赖库：

- **Pandas**: 用于数据读取和结构化处理   

  

- **NumPy**: 用于科学计算和数值处理   

  

- **Matplotlib**: 用于基础绘图和图表展示   

  

- **Seaborn**: 基于 Matplotlib 的高级统计可视化工具   

  

  

## 4. 项目结构

```Plaintext
.
├── 房地产市场分析.ipynb    # 核心分析代码与可视化展示 
├── data/
│   └── house_sales.csv    # 房产销售原始数据集 
└── README.md              # 项目说明文档
```

## 5. 主要分析流程

1. **数据导入**: 加载 `house_sales.csv`，初步查看数据结构 。  

2. **数据预处理**:

   - 清洗 `area`、`price` 等字段中的单位，转换为数值型。
   - 处理缺失值及重复数据。
   - 提取 `year` 字段中的年份信息。

3. **探索性数据分析 (EDA)**:

   - 分析各省份/城市房产挂牌量的分布。
   - 统计不同城市房价的均值、中位数及其分布波动。
   - 使用热力图分析面积、单价与总价的相关性。

4. **数据可视化**: 绘制价格分布直方图、各因素对房价影响的箱线图等 。  

   

   

## 6. 使用说明

1. 确保电脑已安装 Python 及上述依赖库。
2. 将 `house_sales.csv` 放置在与 Notebook 相同目录下的 `data` 文件夹中 。  
3. 在终端运行 `jupyter notebook`。
4. 打开 `房地产市场分析.ipynb` 并依次运行代码单元格。

---



**注意**: 数据集中包含部分缺失值（如 `unit` 或 `year` 为空），代码中已包含相应的处理逻辑 。  